Place browser-ready third-party assets here.

Expected assets for this project:

- `virtual-gamepad-lib/` (copied from submodule `third_party/virtual-gamepad-lib/dist`)

Use `tools/sync_vendor_assets.sh` to build/sync from the submodule.
